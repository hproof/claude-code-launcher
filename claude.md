
# 项目介绍
- 这是一个 vscode 插件项目

# 插件功能
- 加入一个命令 `Launch Claude Code`， 包含以下功能
    - 打开一个终端（默认为 powershell），运行指定命令（默认为 claude）
    - 记录 目录 和 终端 的 对应关系， 如果当前目录 已经有打开的终端， 则把该终端切换到前台，而不是重新打开
- 可配置项
    - 终端类型，例如 默认、cmd、powershell
    - 命令参数，例如 `claude -c`

# AI 规范
- 按需构建项目，不要加入多余功能